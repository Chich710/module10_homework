const arr = [71, 6, 13, 55, 84, 1, 35]

let elements = arr.length

alert('Количество объектов в массиве = ' + elements)

arr.forEach(element => {
  console.log(element);
});

// Задание выполнено не до конца, там нужно было ещё перебрать массив и вывести в консоль каждый элемент. Добавила недостающую часть